{
    "name": "capile/pgp",
    "description": "GPG for encryption and signing",
    "type": "library",
    "license": "GPL-3.0-only",
    "require": {
        "php": ">=7.2"
    },
    "autoload" : {
        "psr-4": {
            "" : "src/"
        }
    },
    "require-dev": {
        "codeception/codeception": "3.1.*"
    },
    "scripts": {
        "test": "vendor/bin/codecept run -v "
    }
}